# the published candidate through the systemd-host runbook

**Result: install exit 0, persist exit 0, uninstall exit 0.** The artifact is
the published, workflow-signed `v2.5.0-rc.7` bundle verified against the
production trust anchor — the first candidate to go through the clean-host
runbook as published rather than as a development-signed rebuild.

With the rollback record
([`v2.5.0-rc.7-rollback-published-linux-aarch64-python3.13.md`](v2.5.0-rc.7-rollback-published-linux-aarch64-python3.13.md)),
every phase of the runbook has now run on this tuple with the published
bundle as the release under test.

## Identity of this run

| | |
| --- | --- |
| Host | Raspberry Pi 4 Model B Rev 1.5, Debian GNU/Linux 13 (trixie), kernel 6.18.34+rpt-rpi-v8, systemd 257, stock Python 3.13.5 |
| Platform tuple | `linux-aarch64-python3.13`, verified on the host by the harness |
| Release under test | `v2.5.0-rc.7`, tag commit `cb021cbca03e02e953d7b8f122cbb524ebca9c46`; bundle `ori-runtime-2.5.0-rc.7-linux-aarch64-python3.13.tar.gz` SHA-256 `9baa17efa4e71abef8635e3d662864c3ce7bab31298e9ad5ccd36c1c62dca7ad`, fetched from the release origin with its `.sha256` and `.signature.json` |
| Trust anchor | the packaged production registry `ori/installer/release-keys.json`, key `ori-runtime-release-2026-01`; nothing substituted |
| Harness revision | 14, SHA-256 `945806e9584d8001275b7543a5a263b2b19026d2fcb49ab647e11f26c1a9455c` |
| Boots | installed on `3aebe4b3-…`; persisted on `5d390653-…` |

## What was proven

- The published bundle verifies against the production key before extraction
  and installs in system scope on a clean host: healthy, config written,
  launcher installed and working, unit active and enabled under
  `ori-runtime`, health socket open, doctor clean including `permissions.code`
  on the real venv.
- After a genuine reboot the unit is active and enabled and the runtime
  reports the release under test.
- Uninstall removes the unit, every managed path and the empty install root,
  retaining only the service account.

## What this does not establish

Nothing about other tuples: `linux-x86_64-python3.12` and Raspberry Pi OS
Bookworm (`aarch64`, 3.11) have published bundles for this candidate and no
installation evidence.

## Complete harness output

[`v2.5.0-rc.7-run-published-systemd-host-linux-aarch64-python3.13.txt`](v2.5.0-rc.7-run-published-systemd-host-linux-aarch64-python3.13.txt),
every phase verbatim.
