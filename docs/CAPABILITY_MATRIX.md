# Capability Matrix

This is the authoritative record of what is real versus planned.

| Capability                                       | Status            | File ref                                  | Notes                                                                                                                 |
| :----------------------------------------------- | :---------------- | :---------------------------------------- | :-------------------------------------------------------------------------------------------------------------------- |
| Tier A/B/C/D dispatch                            | Implemented       | action_dispatcher.py                      | All four tiers, asyncio.shield for Tier D                                                                             |
| Intelligence Elevator Tier 1 (rule engine)       | Implemented       | elevator.py                               | Deterministic. Tier D bypass always.                                                                                  |
| Intelligence Elevator Tier 2 (local SLM)         | Implemented       | elevator.py                               | Qwen2.5-0.5B via llama-cpp-python                                                                                     |
| Causal memory short-circuit cache                | Implemented       | reasoning/causal_memory.py, reasoning/elevator.py, state/store.py | Optional `reasoning.causal_memory.enabled` path short-circuits repeated trigger patterns with cached operator-safe resolutions, reducing LLM variance and token spend while preserving trigger-tier authority. |
| Prompt history placeholder interpolation          | Implemented       | reasoning/elevator.py, tests/test_elevator.py | Supports safe `{history.method(...)}` interpolation in prompts (AST-validated, async-resolved), including compact `history.last_n(...)` output for low-token local SLM reasoning. Unresolved placeholders deterministically substitute `null` sentinel rather than passing raw template tokens to the model. |
| Skill prompt history placeholder guard            | Implemented       | skills/loader.py, tests/test_skill_loader.py  | Skill load now fails fast when any prompt template exceeds 16 `{history.*}` placeholders; unresolved/unsupported placeholders are emitted at WARNING level for operator visibility. |
| Intelligence Elevator Tier 3 (gateway)           | Partial — stubbed | elevator.py:125, :250                     | MQTT request/response not wired. Falls back to local_slm.                                                             |
| Intelligence Elevator Tier 4 (cloud LLM)         | Partial — stubbed | elevator.py:410, :969                     | Returns stub result. No external API call.                                                                            |
| CoAP actuation executor                          | Implemented       | actions/coap.py, runtime.py:312           | Host allowlist enforced in config.py:409.                                                                             |
| CoAP HAL telemetry adapter                       | Implemented       | hal/coap_adapter.py, runtime.py, config.py | Poll-loop + cached-read CoAP telemetry path with circuit breaker, aiocoap context lifecycle shutdown, and `actions.coap.allowed_hosts` boundary enforcement for sensor targets. |
| Tier C approval workflow                         | Implemented       | action_dispatcher.py:649                  | Uses unified alert failover sender (primary + secondary channel) with local-console fallback path when comms are unavailable. |
| Tier C decision logging                          | Implemented       | reasoning/action_dispatcher.py, state/store.py | Persists rich approval/rejection/timeout records with sensor context, history window, skill/trigger metadata, reasoning confidence/model, operator decision latency, safe-default usage, and final action result for future approval-pattern learning. |
| Tier C local console fallback                    | Implemented       | action_dispatcher.py, runtime.py, config.py | When SMS/WhatsApp are unavailable and `actions.local_console.enabled=true`, Tier C approval polls local inbound channel and records `LOCAL:<reply>` in action results. |
| Tier D relay bypass when DevicePolicy restricted | Implemented       | action_dispatcher.py                      | P3-R0b-i. Critical safety fix.                                                                                        |
| SMS via Africa's Talking (IP)                    | Implemented       | sms.py:57, runtime.py                     | send(message, to_number). Requires internet. Inbound SMS webhook path defaults to localhost bind (`127.0.0.1`), requires shared-token auth, and enforces 64KB request-body cap to prevent memory exhaustion. |
| SMS via GSM modem (offline)                      | Implemented       | actions/sms.py, config.py, runtime.py     | `actions.sms.transport` supports `gsm` and `hybrid` failover. Serial AT command flow is offloaded with `asyncio.to_thread` to preserve event-loop safety. |
| Outbound alert outbox + retry                    | Implemented       | runtime.py, state/store.py                | Store-and-retry queue with channel failover routing. Retries switch preferred channel after first failure; Tier D never abandons, non-Tier-D abandons at threshold. |
| Ed25519 community skill verification             | Implemented       | skills/loader.py, skills/signing.py       | Community skills require valid `ed25519:<base64>` signature against Hub trust anchor; trust anchor resolves in safe precedence (`SkillLoader` override → `ORI_HUB_ROOT_PUBLIC_KEY_B64` env → fail-closed sentinel). Bundled local skills unchanged. |
| Python-level skill sandbox                       | Implemented       | loader.py:518, 562                        | RestrictedImportFinder + AST validation.                                                                              |
| OS-level sandbox (seccomp/Landlock)              | Implemented       | os_sandbox.py, loader.py:570, runtime.py  | Community hooks execute in subprocess with seccomp/Landlock on supported Linux kernels; falls back to Python sandbox unless strict mode requires OS sandbox. |
| Capability posture signaling                     | Implemented       | reasoning/capability_posture.py, runtime.py, elevator.py | Structured posture snapshot (sms/whatsapp/gateway/local_slm/relay/internet). Gateway reachability uses heartbeat TTL, not public-IP ping in elevator. |
| LED + buzzer status driver                       | Implemented       | hardware/led_indicator.py, runtime.py, action_dispatcher.py | Priority-arbitrated GPIO signaling with Tier D override and Tier C pending patterns.                                 |
| Secure remote DevicePolicy fetch                 | Implemented       | policy/remote_fetch.py, runtime.py, config.py               | HTTPS + bearer auth + timestamp skew check + monotonic version guard + Ed25519 verification; rejected payloads are persisted to `override_log` as `policy_rejection`. |
| Runtime supply-chain hardening                   | Implemented       | .github/workflows/ci.yml, .pre-commit-config.yaml, requirements.txt, requirements-dev.txt, scripts/check_workflows.py, scripts/bootstrap.sh | CI uses least-privilege permissions, SHA-pinned GitHub Actions and pre-commit hooks, Harden-Runner audit mode, hash-locked dependency installs, `pip-audit`, CycloneDX SBOM artifact generation, and guards against `pull_request_target`, mutable action refs, unauthorized OIDC, and remote script execution. |
| DevicePolicy enforcement layer                   | Implemented       | action_dispatcher.py, device_policy.py    | P3-R0b-iii.                                                                                                           |
| Device policy refresh loop                       | Implemented       | runtime.py, config.py                     | Optional background refresh (`device_policy.refresh_enabled`) with signed verification + cache update; transient network failures are audit-deduped to prevent log spam. |
| Device policy cache (SQLite)                     | Implemented       | state/store.py, runtime.py                | Signed cache with `raw_payload`; startup re-verifies signature from cache using `device_policy.public_key_b64` before apply. |
| State history compaction skew guard              | Implemented       | state/store.py, runtime.py, config.py, ori.yaml.example | Compaction now enforces explicit cutoff ordering and DB-backed backward clock-skew detection across raw/5min/hourly/daily tables using configurable `state.compaction.max_backward_skew_ms` (default 1h). |
| Battery lifecycle observability                  | Implemented       | skills/battery-lifecycle-observer/, runtime.py, tests/test_battery_lifecycle_skill.py | Dual-mode lifecycle monitoring: SOC-based equivalent-full-cycle/deep-discharge heuristics (future PCB path) plus OLAX PoC voltage-decay proxy during outage windows. Runtime-level stale-sensor warning emits Tier A alerts when a sensor is silent beyond 2x poll interval. |
| Health/status RPC socket                         | Implemented       | runtime_health_socket.py, runtime.py, config.py, tests/test_runtime_health_socket.py | Read-only Unix domain socket RPC (`/run/ori/health.sock` default) returns capability posture, sensor status, last alert timestamps, DevicePolicy state, and uptime for local diagnostics (`ori-cli doctor`). |
| Energy anomaly detector v2                       | Implemented       | skills/energy-anomaly-detector/, tests/test_energy_anomaly_skill.py | Baseline-aware derived metrics in hooks (`deviation_percent`, `spike_ratio`, `sustained_high_ratio`, `recent_volatility_percent`) drive plain-language Tier A alerts plus Tier D overcurrent cutoff. Uses shared `ori.skills.composer` for diagnosis sanitization, timezone-safe timestamping, and SMS length policy. |
| Solar performance monitor skill                  | Implemented       | skills/solar-performance-monitor/, tests/test_solar_performance_skill.py | Heuristic daytime solar diagnostics with strict config baselines (`installed_pv_capacity_watts`, `battery_capacity_wh`), local-time gating, and plain-language SMS-first alert composition via shared composer helpers. |
| Energy cost calculator hook                      | Implemented       | skills/energy-anomaly-detector/hooks.py, tests/test_energy_anomaly_skill.py | Deterministic cost math computes both observed window cost and projected `/day` run-rate risk. Currency/voltage inference uses explicit config first, then country-code defaults, with `exact`/`estimated` confidence labeling. |
| Offline Tier C auth tokens                       | Implemented       | security/offline_tokens.py, action_dispatcher.py, state/store.py | Local-console `TOKEN:<value>` approvals are signature-verified (Ed25519), device/action-bound, expiry-checked, and replay-protected via SQLite token claim table. |
| Authenticated remote runtime commands            | Implemented       | security/remote_commands.py, security/remote_command_throttle.py, actions/sms.py, actions/whatsapp.py, state/store.py, runtime.py | Structured SMS and WhatsApp remote commands are intercepted before approval-message handling, HMAC-SHA256 verified, timestamp checked, replay-protected, audited to SQLite with sender identity, and fail-closed on Tier D/safety-disabling mutations or direct actuator commands. Repeated rejected commands from the same channel/sender suppress generic rejection feedback after the abuse threshold while audit logging continues. |
| Remote command execution policy                  | Implemented       | security/remote_command_policy.py, runtime.py, state/store.py, policy/remote_fetch.py, security/remote_command_responses.py | Authenticated commands pass through a runtime-owned execution policy. `REFRESH_POLICY` executes the signed DevicePolicy refresh path; `APPLY_POLICY` fetches an HTTPS-referenced bundle, matches SHA-256, and reuses signed DevicePolicy verification. `SET_THRESHOLD` is executable with Tier D startup-sensitivity and active-suppression guards. SMS/WhatsApp return concise operator feedback for execution outcomes. All other commands remain audit-only. |
| SET_THRESHOLD remote command                     | Implemented       | security/threshold_guard.py, runtime.py   | Tier D threshold keys are protected by a startup-sensitivity guard: remote changes may make Tier D more sensitive but not less sensitive than startup config. Direction-ambiguous Tier D expressions fail closed. Active-suppression checks run against recent sensor readings. |

## Recent updates

- 2026-05-07: Runtime async offload refactor changed blocking-call wrappers from `run_in_executor` to `asyncio.to_thread` in actions/HAL/reasoning/store paths. Capability statuses unchanged (implementation detail only).
- 2026-05-08: Added safe history-placeholder prompt interpolation in the elevator and hardened state compaction with configurable DB-backed clock-skew guard (`state.compaction.max_backward_skew_ms`).
- 2026-05-09: Added load-time guardrails for history placeholder volume in skill prompts and raised unresolved history placeholder observability to WARNING logs.
- 2026-05-11: Hardened runtime control paths: DevicePolicy health snapshot now reads `is_expired` property correctly, deduplicator restarts suppression windows after expiry, and inbound SMS webhook defaults to loopback bind for safer network posture.
- 2026-05-11: Threaded pre-evaluated rule results through elevator tier selection to remove redundant rule evaluation inside `reason()`; community-skill trust anchor is now configurable at runtime (constructor/env) while preserving fail-closed verification defaults.
- 2026-05-11: Wired causal-memory into elevator reasoning as an optional pre-LLM cache (`reasoning.causal_memory.enabled`) and aligned keying to event timestamp (UTC) for deterministic pattern reuse across hosts.
- 2026-05-14: Hardened runtime supply chain: CI now uses explicit least-privilege permissions, SHA-pinned actions and pre-commit hooks, Harden-Runner audit mode, hash-locked installs, `pip-audit`, CycloneDX SBOM artifact generation, Python 3.12 lock generation, and a guard for mutable actions/OIDC/remote script execution.
- 2026-05-30: Added Tier C decision logging for full proposal, operator decision, safe-default, and outcome context needed for future approval/rejection learning.
- 2026-05-30: Added authenticated remote command verification with HMAC signatures, timestamp skew checks, replay audit, and Tier D/safety mutation denial.
- 2026-05-31: Generalized authenticated remote command ingress across SMS and WhatsApp so structured commands cannot be mistaken for Tier C approval replies.
- 2026-05-31: Added remote command execution policy and execution audit logging; `REFRESH_POLICY` executes the existing signed refresh path and `APPLY_POLICY` executes only through fetch-by-reference with SHA-256 content binding and signed DevicePolicy verification.
- 2026-05-31: Added SET_THRESHOLD remote command with Tier D startup-sensitivity and active-suppression invariants enforced by AST-based key identification, direction-aware threshold checks, and StateStore sensor history checks.
- 2026-06-01: Added SMS/WhatsApp remote command execution feedback so authenticated command outcomes are acknowledged to operators without changing execution audit semantics.
- 2026-06-01: Added sender-scoped remote command rejection-feedback throttling. Repeated rejected commands continue to be audited, but SMS/WhatsApp generic rejection replies are suppressed after the threshold to reduce abuse and oracle risk.
