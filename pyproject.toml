[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ori-runtime"
version = "2.5.0rc3"
description = "Offline-first agentic IoT runtime — give physical devices the ability to reason"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Ori Nexus Systems LTD", email = "wasiubakare@gmail.com" }
]
keywords = ["iot", "edge-ai", "agentic", "llm", "raspberry-pi", "offline-first"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Hardware",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.11"

dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
eval = []
runtime = [
    "paho-mqtt>=2.0",
    "psutil>=5.9",
    "pyserial>=3.5",
    "pysolarmanv5>=3.0",
    "asyncua>=1.1",
    "aiofiles>=23.0",
    "aiohttp>=3.14.1",
    "httpx>=0.28",
    "cryptography>=48.0.1",
    "pyopenssl>=26.3.0",
    "twilio>=9.0",
    "africastalking>=1.0",
    "python-dotenv>=1.0",
    "urllib3>=2.7.0",
]
phone = [
    "pyserial>=3.5",
    "tzdata>=2025.2",
    "httpx>=0.28",
    "cryptography>=48.0.1",
    "twilio>=9.0",
    "africastalking>=1.0",
]
phone-growatt = [
    "pyserial>=3.5",
    "tzdata>=2025.2",
    "httpx>=0.28",
    "cryptography>=48.0.1",
    "twilio>=9.0",
    "africastalking>=1.0",
    "pysolarmanv5>=3.0",
]
phone-victron = [
    "pyserial>=3.5",
    "tzdata>=2025.2",
    "httpx>=0.28",
    "cryptography>=48.0.1",
    "twilio>=9.0",
    "africastalking>=1.0",
    "aiomqtt>=2.0",
]
pi = [
    "gpiozero>=2.0",
    "smbus2>=0.4",
    "RPi.GPIO>=0.7",
]
llm = [
    "llama-cpp-python>=0.2",
]
cloud = [
    "anthropic>=0.28",
]
all = [
    "paho-mqtt>=2.0",
    "psutil>=5.9",
    "pyserial>=3.5",
    "pysolarmanv5>=3.0",
    "asyncua>=1.1",
    "aiofiles>=23.0",
    "aiohttp>=3.14.1",
    "httpx>=0.28",
    "cryptography>=48.0.1",
    "pyopenssl>=26.3.0",
    "twilio>=9.0",
    "africastalking>=1.0",
    "python-dotenv>=1.0",
    "urllib3>=2.7.0",
    "gpiozero>=2.0",
    "smbus2>=0.4",
    "RPi.GPIO>=0.7",
    "llama-cpp-python>=0.2",
    "anthropic>=0.28",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "pre-commit>=3.7",
    "ruff>=0.4",
    "mypy>=1.18",
    "types-PyYAML>=6.0",
]

[project.scripts]
ori = "ori.cli:main"
ori-runtime = "ori.runtime:main"
ori-config-install = "ori.config_installer:main"
ori-phone-doctor = "ori.phone_doctor:main"
ori-inverter-profile-doctor = "ori.inverter_profile_doctor:main"
ori-firmware-provisioner = "ori.firmware_provisioner:main"
ori-install-linux = "ori.installer.cli:main"

[project.urls]
Homepage = "https://github.com/ori-platform/ori-runtime"
Repository = "https://github.com/ori-platform/ori-runtime"
Documentation = "https://github.com/ori-platform/ori-runtime#readme"
Issues = "https://github.com/ori-platform/ori-runtime/issues"


[tool.setuptools.data-files]
"share/ori-runtime/skills/battery-lifecycle-observer" = [
    "skills/battery-lifecycle-observer/skill.yaml",
    "skills/battery-lifecycle-observer/hooks.py",
]
"share/ori-runtime/skills/energy-anomaly-detector" = [
    "skills/energy-anomaly-detector/skill.yaml",
    "skills/energy-anomaly-detector/hooks.py",
]
"share/ori-runtime/skills/hvac-refrigerant-monitor" = [
    "skills/hvac-refrigerant-monitor/skill.yaml",
    "skills/hvac-refrigerant-monitor/hooks.py",
]
"share/ori-runtime/skills/pc-network-threat-monitor" = [
    "skills/pc-network-threat-monitor/skill.yaml",
    "skills/pc-network-threat-monitor/hooks.py",
]
"share/ori-runtime/skills/pc-system-health" = [
    "skills/pc-system-health/skill.yaml",
    "skills/pc-system-health/hooks.py",
]
"share/ori-runtime/skills/prosumer-energy-advisor" = [
    "skills/prosumer-energy-advisor/skill.yaml",
    "skills/prosumer-energy-advisor/hooks.py",
]
"share/ori-runtime/skills/retail-occupancy-optimizer" = [
    "skills/retail-occupancy-optimizer/skill.yaml",
    "skills/retail-occupancy-optimizer/hooks.py",
]
"share/ori-runtime/skills/site-safety-ppe" = [
    "skills/site-safety-ppe/skill.yaml",
    "skills/site-safety-ppe/hooks.py",
]
"share/ori-runtime/skills/solar-performance-monitor" = [
    "skills/solar-performance-monitor/skill.yaml",
    "skills/solar-performance-monitor/hooks.py",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["ori*"]

[tool.setuptools.package-data]
ori = ["py.typed", "hal/inverter_profiles/*.yaml", "installer/release-keys.json"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "hardware: requires real Raspberry Pi hardware",
    "slow: builds a real virtual environment from locked dependencies",
    "disclosure_release: evidence-disclosure checks needing real release inputs",
]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

# `tests/` imports a few helpers from `scripts/`, which is not a package and so
# is not on the analyzer's path by default. Two test modules already did this
# and reported an unresolved import in the editor; declaring the path once
# fixes both rather than scattering per-file suppressions.
[tool.pyright]
# Without an explicit include, pyright resolves `ori/hal/x.py` a second time as
# `hal/x.py` through the package root and reports every diagnostic twice. The
# duplicate is invisible in a total and makes the count untrackable.
include = ["ori", "tests", "scripts"]
extraPaths = ["scripts"]

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
ignore_missing_imports = true
no_implicit_optional = true
pretty = true
show_error_codes = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
# llama-cpp-python is optional and absent from the hash-locked CI environment.
# With it installed the ImportError fallback rebinds an imported class and needs
# an ignore; without it the same ignore is unused. The comment cannot be correct
# in both, so this module opts out of the unused-ignore check rather than
# carrying a comment whose correctness depends on which extras are present.
module = ["ori.reasoning.local_llm"]
warn_unused_ignores = false

[[tool.mypy.overrides]]
module = [
    "ori.gateway.*",
    "ori.integration.*",
    "ori.network.events",
    "ori.reasoning.rule_engine",
    "ori.skills.hooks_api",
    "ori.skills.loader",
    "ori.policy.*",
    "ori.reasoning.capability_posture",
    "ori.reasoning.escalation_policy",
    "ori.security.gateway_messages",
    "ori.security.remote_commands",
]
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = [
    "ori.hal.*",
    "ori.hardware.*",
    "ori.skills.sandbox",
    "ori.skills.os_sandbox",
    "tests.*",
]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["yaml"]
ignore_missing_imports = true
